{% extends "base.jinja" %}

{% block content %}
RUN \
    pip --no-cache-dir install \
        mxnet{% if arch == "gpu"  %}-{{ cuda_version }}{% endif %}=={{ _version }} \
        tensorboardX \
    && rm -rf /tmp/* \
    && rm -rf /root/.cache
{%- endblock %}
